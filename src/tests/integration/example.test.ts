/**
 * Carpeta: tests/integration
 * 
 * Esta carpeta contiene pruebas de integración que testean la interacción
 * entre múltiples unidades (componentes + servicios, hooks + API, etc.).
 * 
 * Ejemplos: LoginFlow.test.tsx, UserAPI.integration.test.ts
 * 
 * Características:
 * - Testean la interacción entre módulos
 * - Pueden usar mocks de API pero testean flujos completos
 * - Validan que los componentes funcionan bien juntos
 * - Más lentas que las unitarias pero más rápidas que e2e
 */

// Placeholder for integration tests.
